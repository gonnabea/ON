const HOME = "/home"
const CHAT = "/chat"
const CHATROOM = "/chatroom"
const API = "/api"
const LOGIN = "/login"
const LOGOUT = "/logout"
const JOIN = "/join"
const FINDCHAT = "/find-chat"

const routes = {
  home: HOME,
  chat: CHAT,
  chatroom: CHATROOM,
  api: API,
  login: LOGIN,
  logout: LOGOUT,
  join: JOIN,
  findChat: FINDCHAT,
}

export default routes
